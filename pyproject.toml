[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imview"
version = "0.4.0"
description = "Image viewers for geospatial data"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "David Shean", email = "dshean@gmail.com"}
]
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "gdal",
    "matplotlib",
    "pygeotools",
    "matplotlib-scalebar",
    "matplotlib-colorbar",
]

[project.urls]
Homepage = "https://github.com/dshean/imview"

[project.scripts]
imviewer = "imview.imviewer:main"
stack_view = "imview.stack_view:main"
review_img = "imview.review_img:main"
color_hs = "imview.color_hs:main"

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.package-data]
"imview.lib" = ["*.cpt"]
